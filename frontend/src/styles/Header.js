import styled from "styled-components"

const Header = styled.header`
  /* Mimic the Bootstrap h2 tag, mostly, but without hierarchy. */
  font-size: 2rem;
  font-weight: 500;
  line-height: 1.2;
`

export default Header
